name: Publish to PyPI

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (build + test only, no publish)"
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  id-token: write

jobs:
  e2e-gate:
    name: E2E Tests Gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e .

      - name: Run all e2e tests
        run: bash tests/run-all.sh

  pytest-gate:
    name: Python Test Gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -e ".[dev,all]"

      # The shell e2e suite is self-skipping by design (it skips when platforms
      # are unconfigured, and the destructive lifecycle test is opt-in), so on
      # its own it can go green having checked almost nothing. This gate is what
      # actually holds a release to the Python suite.
      - name: Run pytest
        run: pytest -q

  publish:
    name: Build & Publish
    needs: [e2e-gate, pytest-gate]
    runs-on: ubuntu-latest
    environment: pypi-release
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build twine

      - name: Build package
        run: python -m build

      - name: Verify package
        run: twine check dist/*

      - name: Check version not already published
        run: |
          set -euo pipefail
          VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          if pip index versions telechatai 2>/dev/null | grep -q "$VERSION"; then
            echo "telechatai@${VERSION} is already published on PyPI."
            exit 1
          fi
          echo "Publishing telechatai@${VERSION}"

      - name: Publish to PyPI
        if: ${{ !inputs.dry_run }}
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
        run: twine upload dist/*
